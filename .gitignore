# Cityscapes dataset — the download cell extracts ~11GB here (data/ exists, kept empty in git)
data/

# Checkpoints written by train_one_variant(): models/{run}.pt and {run}_resume.pt
models/*.pt

# Jupyter checkpoint copies of CitySegmentation.ipynb
.ipynb_checkpoints/

# Ruff cache — ruff is configured in pyproject.toml and run via the Makefile / pre-commit
.ruff_cache/

# PyCharm project files (.idea/ is present in this working tree)
.idea/
